# LSOF - примеры использования
Утилита `lsof` умеет отображать процессы, которые работают с определенным файлом или сокетом.

Список всех сетевых соединений

    lsof -i

Список процессов, работающих с портом 80

    lsof -i :80
